<%- include('../partials/head') %>

    <div class="containerMiniForm">
        <div class="container">

            <% if (locals.errors) {%>
                <ul>
                    <% errors.forEach((error)=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }); %>
                </ul>
                <% } %>
                    <form class="dark-form" action="/tags/add" method="POST">
                        <div class="form-group">
                            <label for="name_tag">Name Tag</label>
                            <input type="text" name="name_tag" id="name_tag" placeholder="Tag name">

                        </div>
                        <button type="submit" class="btn btn-primary" style="justify-self: center;">Add tag</button>
                    </form>
        </div>

    </div>

    <%- include('../partials/footer') %>