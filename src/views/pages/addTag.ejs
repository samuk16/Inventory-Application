<%- include('../partials/head') %>

    <div id="addModal">
        <div class="modal-content">

            <% if (locals.errors) {%>
                <ul>
                    <% errors.forEach(function(error) { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }); %>
                </ul>
                <% } %>
                    <form class="dark-form" action="/tags/add" method="POST">
                        <div class="form-group">
                            <label for="name_tag">Name Tag</label>
                            <input type="text" name="name_tag" id="name_tag" placeholder="Tag name">

                        </div>
                        <button type="submit" class="btnSubmit">Add tag</button>
                    </form>
        </div>

    </div>

    <%- include('../partials/footer') %>